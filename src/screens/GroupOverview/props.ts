import {StackScreenProps} from '@react-navigation/stack';
import {GroupStackParamList} from '../../navigators/GroupStack';

export type Props = StackScreenProps<GroupStackParamList, 'GroupOverview'>;

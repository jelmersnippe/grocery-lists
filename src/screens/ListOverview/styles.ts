import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    noListsText: {
        textAlign: 'center',
        marginTop: 20,
        fontWeight: 'bold',
        fontSize: 24
    }
});

export default styles;

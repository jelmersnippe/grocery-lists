import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        borderTopWidth: 1,
        borderBottomWidth: 1,
        borderColor: 'black'
    },
    button: {
        borderLeftWidth: 1,
        borderRightWidth: 1,
        borderColor: 'black'
    },
    input: {
        height: 40,
        width: 40,
        textAlign: 'center'
    }
});

export default styles;

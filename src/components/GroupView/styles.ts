import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    wrapper: {
        marginHorizontal: 10,
        marginVertical: 5
    },
    title: {
        fontWeight: 'bold',
        fontSize: 20
    },
    groupContainer: {
        flexDirection: 'row',
        alignItems: 'center'
    }
});

export default styles;

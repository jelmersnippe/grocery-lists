import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    wrapper: {
        flexDirection: 'row',
        marginBottom: 5
    },
    name: {
        flex: 1,
        fontSize: 18,
        marginRight: 10
    }
});
